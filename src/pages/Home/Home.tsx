import styles from "./Home.module.css";
import { Button } from "antd";

export default function Home() {
    return (
        <div>
            <Button type="primary">
                Voir les données
            </Button>
        </div>
    );
}